create database company_new;
use company_new;
create table employees(id int primary key,
						f_name varchar(50),
                        l_name varchar(50),
                        age int,
                        proff varchar(50),
                        country varchar(50));
